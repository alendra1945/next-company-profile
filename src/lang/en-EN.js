/* 
    Use <type>.<section>.<componentName> as key lang
    type = type for key (ex: title, subtitle, etc)
    section = number or name of section (ex: 1, 2, header, etc)
    componentName = name of component of page (ex: home, profile, etc)
*/
module.exports = {
  title: 'helel',
};
